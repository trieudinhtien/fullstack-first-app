export const themeConfig = (mode: boolean) => ({
  palette: {
    mode: mode ? "dark" : "light",
  },
  typography: {
    fontFamily: '"Inter", "Roboto", "Helvetica Neue", sans-serif',
  },
  components: {},
});
